function verificarIdade(idade) {
    if (idade >= 18) {
        console.log('Maior de idade: ' + idade);
    } else {
        console.log('Menor de idade: ' + idade);
    }
}

verificarIdade(11);
